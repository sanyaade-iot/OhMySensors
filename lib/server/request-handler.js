module.exports = async function(options, ws) {
  var id = options.id;
  var type = options.type;
  var params = options.params;
  
  switch (type) {
    case 'getAverages':
      break;
    case 'getLastChanges':
      break,
    default:
      break;
  }
}